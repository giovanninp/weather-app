import {DefaultTheme} from "react-native-paper";

export default {
    ...DefaultTheme,
    roundness: 10,
  colors: {
    ...DefaultTheme.colors,
    primary: '#3498db',
    accent: '#f1c40f',
  },
}